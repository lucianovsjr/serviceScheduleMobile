const INITIAL_STATE = {
  name: '',
  email: '',
};

function user(state = INITIAL_STATE, action) {
  switch (action) {
    case '':
      return state;
    default:
      return state;
  }
}

export default user;
