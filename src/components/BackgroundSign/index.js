import LinearGradient from 'react-native-linear-gradient';
import styled from 'styled-components/native';

export default Background  = styled(LinearGradient).attrs({
  colors: ['#97c6f4', '#60acf5' ]
})`
  flex: 1;
`;
